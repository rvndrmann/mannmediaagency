
import { canvasTool } from './canvas-tool';
import { ToolDefinition } from '../types';

// Export default tools array
export const defaultTools: ToolDefinition[] = [
  canvasTool
];

// Export individual tools for direct import
export { canvasTool };
